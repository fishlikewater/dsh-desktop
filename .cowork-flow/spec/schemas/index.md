# Schema Specs

本目录存放 JSON Schema。

- `adapter.schema.json`: 宿主适配器声明 schema。
- `party-mode-v2-actions.schema.json`: Party Mode V2 action schema。

Schema 只描述数据合同，不承载流程说明；流程和行为边界写入 `contracts/`、Skill command manifests 或 `AGENTS.md`。
